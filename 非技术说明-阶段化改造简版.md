5.5

## 本次更新内容

### 一、生成完成后可以手动改剧本

剧本生成完之后，页面会直接显示可以编辑的剧本阅读器。用户不再只是"看结果"，而是可以在页面里直接改内容。具体支持：

- **改台词**：每条对话的内容可以直接在文字框里编辑
- **改场景概述**：每幕开头的事件描述也可以修改
- **改说话人**：下拉菜单选，列表来自已生成的角色，不需要手动输入
- **改场景名**：下拉菜单选，列表来自系统里已有的场景，不需要手动输入
- **改动作**：每条对话下面的角色动作，角色名和动作名都是下拉菜单，从现有数据里选
- **改镜头手法**：每条对话可以选景别（全景/中景/近景等）和镜头衔接方式（Cut/Ease In 等），以及填写镜头描述
- **添加/删除对话行**：每幕里可以插入新的对话，或者删掉不要的
- **添加/删除整幕**：可以在剧本末尾添加新的一幕，或者删除某一幕
- **添加时自动填默认值**：新增对话或新增幕时，角色、场景、动作都会自动填入第一个可选项，不会出现空白字段

所有改动都保存在页面里，点"下载剧本"按钮导出的就是改动后的版本。

---

### 二、可以控制生成几幕

生成剧本之前，可以用加减按钮设置想要的幕数（1 到 10 幕）。这个设置会传给 AI，AI 会严格按照指定的幕数生成，不会多也不会少。幕数超过 5 时，页面会提示"生成时间会更长"，方便用户提前有预期。

---

### 三、历史记录面板

页面右上角新增"历史记录"按钮，点开会从右侧滑出一个面板，列出所有历史生成会话。每条记录显示：

- 会话名称（可以起自定义名字）
- 生成时间
- 当时选的场景和幕数
- 操作按钮：**加载剧本**（把这次的剧本载入编辑器）、**下载 Word**（直接下载该次的 Word 版本）

会话名称支持双击直接在列表里改名，改完失焦自动保存。

---

### 四、为每次生成命名版本

剧本生成成功之后，结果区域会出现一个命名输入框。可以给这次生成起一个自定义名字（比如"第一稿-情感路线"），点保存之后，历史记录面板里就能看到这个名字。方便区分多次生成的不同版本。

---

### 五、下载 Word 版本

结果区域新增"下载 Word 剧本"按钮。下载的 Word 文件只保留人类可读的内容：

- 每幕的场景标题
- 场景核心事件描述
- 每条对白（角色名：台词）
- 镜头描述文字

不包含技术字段（景别代码、切换方式、站位坐标等），适合给非技术人员阅读或打印。

---

### 六、修复：动作细节为空不再触发重新生成

之前 AI 生成的剧本里，动作细节（`motion_detail`）字段经常是空的，但系统把空值当成错误，会反复要求 AI 重填，导致不必要的返工循环。本次修复后，这个字段允许为空，不再触发重生成。

---

4.21

## 智能体协作全流程说明

### 一共有哪些智能体？

整个剧本生成过程，由一组 AI 智能体分工合作完成，就像一个真实的剧组——从创意开发到最终剧本，每个岗位各司其职。

| 智能体 | 类比现实岗位 | 负责的事 |
|--------|------------|---------|
| 概念智能体 | 创意总监 | 提炼故事的一句话核心、基调和核心冲突 |
| 梗概智能体 | 故事编辑 | 在核心概念的基础上，写出有头有尾的故事梗概 |
| 人物小传智能体 | 角色设计师 | 给每个主要角色写目标、弧光和人物关系 |
| 分场大纲智能体 | 剧本策划 | 把故事拆成一场场，规划每场戏的目标和转折 |
| 导演智能体 | 编剧 / 导演 | 根据以上所有素材，生成完整的剧本 JSON 初稿 |
| 批评智能体 | 剧本顾问 | 审查剧情逻辑、节奏和冲突是否合理 |
| 对白智能体 | 对白编辑 | 专门审查台词质量和人物语气是否一致 |
| 摄影指导智能体 | 摄影指导 | 剧本初稿完成后，补充镜头语言和摄像机分配 |

---

### 他们是怎么协作的？

可以把这个过程理解为"接力写作"——每个智能体都在上一棒的成果基础上继续深化，而不是各自独立发挥。

**第一棒：打地基（概念 → 梗概 → 人物 → 大纲）**

用户填写场景、角色和创作想法后，首先由概念智能体提炼出"这个故事到底想说什么"，然后把这个结论交给梗概智能体，梗概智能体在此基础上写出故事骨架，再交给人物小传智能体完善角色，最后由分场大纲智能体把骨架分解成每一场的节拍。

这个阶段的核心价值在于：后面的导演智能体不再是"凭感觉瞎写"，而是有完整的结构支撑。

**第二棒：写初稿（导演智能体）**

导演智能体拿到前四棒的全部成果——核心概念、故事梗概、人物小传、分场大纲——综合起来生成剧本 JSON 初稿。它就像一个编剧拿着详细的故事圣经开始落笔，写出具体的对白、动作和场面调度。

**第三棒：审稿和修改（批评 + 对白智能体，最多循环 3 轮）**

初稿出来后，批评智能体和对白智能体会分别从剧情逻辑和台词质量两个维度提意见，如果发现问题，就反馈给导演智能体修改。这个过程最多来回三轮，类似于编剧和制片人之间的审稿-返修流程。

**第四棒：技术校验（自动检查）**

审稿通过后，系统会做一轮自动技术检查，验证角色名、站位等关键字段是否合规。如果不合规，会直接告诉导演智能体哪里有问题，让它修正，最多修复三次。这一步纯粹是质量把关，不做创作判断。

**第五棒：摄影指导（可选后处理）**

剧本内容确定后，摄影指导智能体介入，负责决定每一幕用什么镜头、摄像机放在哪里、角色站在场景的什么位置。它不改台词，只填拍摄层面的信息——类似于导演定好对白之后，摄影师来设计机位和构图。

> **注：摄影指导智能体目前处于接入阶段，内部逻辑还在和刘艳对接调整中，现阶段以跑通流程为主，后续会持续完善。**

---

### 上下文是怎么传递的？

每个智能体在工作时，都能看到前面所有智能体的输出结果。也就是说：

- 梗概智能体能看到"核心概念"
- 人物小传智能体能看到"核心概念 + 梗概"
- 分场大纲智能体能看到前三棒的全部内容
- 导演智能体能看到前四棒的全部内容

这样做的目的是保持一致性——越往后的智能体掌握的背景越完整，写出来的内容越不容易偏题。

---

## Schema 校验与自动返修机制

### 为什么需要 Schema 校验？

AI 生成的内容是"自由发挥"的，但剧本最终要被 Unity 引擎读取和执行，所以每个字段的格式必须精确——比如"镜头切换方式"不能填"fade in"，必须是系统认识的 `"Ease In"`；"景别"不能填"特写"，只能是规定列表里的词。

如果 AI 随意发挥填了不规范的内容，下游引擎直接不认，就会出错。Schema 校验的作用，就是在剧本交出去之前充当最后一道质检关卡——就像出版社在书稿付印前要做的格式审校，不管内容多好，格式不符合排版规范就得改。

---

### 具体校验了什么？

系统里定义了一套"字段规则表"（Pydantic Schema），对剧本里每一幕的镜头相关字段做约束：

**人物镜头（shot = "character"）必须满足：**

| 字段 | 含义 | 合法值范围 |
|------|------|----------|
| `shot_blend` | 镜头切换方式 | Cut / Ease In / Ease Out / Ease In Out / Hard In / Hard Out / Linear / Custom |
| `shot_type` | 景别 | 全景 / 中景 / 中近景 / 近景 / 第一人称镜头 / 肩后镜头 / 侧跟镜头 / 环绕镜头 / 仰拍镜头 / 俯拍镜头 |
| `Follow` | 镜头是否跟随角色移动 | 0 或 1（整数） |

**场景镜头（shot = "scene"）必须满足：**

| 字段 | 含义 | 合法值范围 |
|------|------|----------|
| `shot_blend` | 镜头切换方式 | 同上 |
| `camera` | 摄像机编号 | 整数 |

此外，**站位规划**的结果也有单独校验：每个角色的点位名、所在区域名、角色名不能为空字符串。

---

### 校验分两个时间点触发

**时间点一：导演初稿完成后（结构检查）**

这时只检查"字段有没有"，而不管填的是什么值。比如 `shot_blend` 这个字段有没有出现、`shot` 是不是填了 `"character"` 或 `"scene"`。

类比：相当于检查合同有没有缺页、有没有漏签名，不管合同内容写得对不对。

**时间点二：摄影指导完成后（内容检查）**

这时字段已经补全，开始严格检查字段值是否合规。比如 `shot_type` 填了"特写"，这个词不在合法列表里，就会报错。

类比：相当于检查合同里每一条款的表述是否符合法律规范，不是随便写几个字就行的。

---

### 出错了怎么处理？返修流程

发现错误之后，系统不会直接中断报错，而是把问题整理成一段清晰的"修改意见"，发给导演智能体，让它针对性地修正。

**修改意见的格式大概是这样：**

> 场景0 角色A：shot_type 非法值 "特写"，可选: ['全景', '中景', '近景', ...]
> 场景1 角色B：shot_blend 非法值 "fade in"，可选: ['Cut', 'Ease In', ...]

注意这里是**精准定位到具体场景、角色、字段、填了什么不对的内容**，而不是含糊地说"有错误请修改"。给 AI 的反馈越精确，它修正的成功率就越高。

收到修改意见后，导演智能体只需修正有问题的那几幕，系统会把修正后的内容打补丁回来，再做一轮校验。整个返修最多重试一次，超出次数就用当前版本继续走，不会卡死。

站位规划的返修逻辑完全一样：规划完成后检查空值，有问题就精准告知哪个角色哪个字段有问题，让模型重填，最多重试两次。

这个机制的核心思路就是：**不让 AI 猜"哪里错了"，而是直接告诉它**——就像改作文的老师在旁边批注，比只打个叉让学生自己领悟效率高得多。
