# 剧本生成流程升级说明（非技术版）

## 升级内容概述

本次将“直接生成剧本”的方式调整为分阶段流程：

- 先定核心一句话（Logline）
- 再写故事梗概（Synopsis）
- 再补人物小传（Character Bios）
- 再做分场大纲（Treatment）
- 最后才生成剧本初稿并继续审查

整体上，流程从“直接产出”改为“先结构化规划、再生成细节”，稳定性与可控性有所提升。

## 用户可见变化

- 日志面板现在能看到“分阶段”的进展，不再是一段段难读的原始 JSON。
- 每个阶段会显示更容易理解的内容（例如核心冲突、人物目标、分场节拍等）。
- 剧本日志里除了对白，也能看到镜头、动作、站位、氛围等关键信息。

## 对用户使用的影响

- 使用入口和填写方式不变，还是原来的操作流程。
- 最终下载文件类型不变，依然是：
  - `script_*.json`
  - `actors_profile_*.json`
  - `position_*.json`（有条件生成）
- 成功返回结构保持不变，不影响现有对接。

## 关于“坐标文件按钮不见了”的问题

本次同步修复了两个体验相关问题：

- 下载坐标文件按钮现在会始终显示；
- 如果本次没有生成坐标文件，按钮会置灰并提示原因，而不是直接消失。

另外还修复了一个常见场景命名问题：  
`Space Station` 和 `SpaceStation` 这类命名差异现在会自动兼容匹配，不会再因为文件名风格不同而直接跳过坐标生成。

## 一句话总结

本次升级的目标是提升生成过程的可解释性与稳定性，同时保持用户原有使用方式与输出格式不变。

---

## 新一轮修复说明

### 解决了什么问题？

本次修复主要针对以下两类情况：

**1. 生成的剧本里出现了不该有的场景名**

之前 AI 在生成剧本时会自己填写"这个场景叫什么"，有时填的名字与用户实际选择的场景不一致。  
现在已修复：场景名称固定取用户选择的那个，AI 不再有权自行发挥。

**2. 生成的剧本里角色没有站位，或者站位是"Position 1"这样的占位符**

剧本生成过程中会有一个专门负责分配"谁站在哪里"的步骤。但如果这个步骤出了问题，剧本里就会残留未被替换的占位符，导致后续无法正常使用。  
本次做了两层保障：

- **AI 层面**：在给 AI 的指令中明确强调"每段对白/动作都必须填写角色站位，不能省略"，并把场景里所有合法的站位名称列给 AI 参考，让它改得准确。
- **兜底层面**：无论 AI 最终输出结果如何，系统会在最后做一次自动扫描。发现任何残留的占位符或无效站位，都会自动替换为场景里真实存在的位置，确保最终文件里没有"坏数据"。

### 对你有什么影响？

- 使用方式没有任何变化。
- 生成成功率更高，出现"位置无法生成"报错的概率降低。
- 最终下载的剧本文件和坐标文件，内容会更准确、更干净。

---

## 摄影指导智能体说明

### 它是做什么的？

剧本初稿写完之后，会有一个独立的"摄影指导"环节自动运行。它的任务是把剧本里每一幕的**画面拍法**填写完整——不是改对白，而是决定：

- 这一幕用什么镜头？（全景、近景、第一人称……）
- 镜头之间怎么切换？（直切、淡入淡出……）
- 摄像机从哪个机位拍？
- 角色站在场景的哪个区域？
- 这一幕画面里，各个角色的空间关系是怎样的？（谁在前景、谁在背景、谁是视觉焦点……）

### 它分几步走？

摄影指导环节内部分三步，依次进行：

**第一步：逐幕分析互动关系，生成画面描述**

对每一幕对白或动作，先分析"谁是主角、谁是配角、谁在旁观"，再用一两句话写出这一幕的镜头描述（比如"A 站在左侧前景，B 在右侧中景，C 退至背景旁观"）。每四幕打包给 AI 处理一次，效率更高。

**第二步：规划角色站位**

根据场景的区域布局和第一步的画面描述，决定每个角色站在哪个区域、靠近哪个标志物、朝向哪里。这一步涉及两次 AI 判断——先分组（谁和谁站在一起），再规划具体位置。

**第三步：分配摄像机**

根据前两步的结果，决定每一幕用哪个机位，并对整个剧本的摄像机分配做整体优化，避免连续很多幕都用同一个机位。

### 关于"场景锚点"是什么

场景文件里记录了一批"锚点"，比如"茶台旁"、"石桥中央"、"神坛前"。这些**不是**角色可以站的固定坐标，而是**场景里标志性物体的位置**。摄影指导用这些锚点来描述角色大概站在哪里（"靠近茶台旁边"），具体的精确坐标由后续独立模块计算，不在这个环节处理。

编剧 AI 在写剧本时也会参考这些区域名称，用来描述每幕发生在场景的哪个部分，但不会直接指定角色的精确位置。

### 前端界面变化

场景选择页面原来显示的是"可用点位"，现在改为显示**场景区域与锚点**，与上述逻辑保持一致——展示的是区域名称和场景里的标志性内容物，而不是预设的角色站位格子。

---

## 本轮主要改动说明

### 1. 移除了"位置智能体"

原来在摄影指导之前有一个独立的"位置智能体"，专门负责决定角色站哪里。后来发现摄影指导的第二步（站位规划）已经完整覆盖了这项工作，两者重叠。因此这个独立的位置智能体已经撤掉，流程更简洁。

### 2. 镜头字段由编剧填、摄影指导补完

剧本里每一幕都有几个"镜头字段"，分工如下：

- **编剧 AI**负责填写：这幕是人物镜头还是场景镜头、用什么镜头切换方式、用什么景别（全景/近景/……）、镜头是否跟随角色移动
- **摄影指导**负责填写：镜头画面描述（用一两句话描述这幕的空间构图）、具体摄像机编号

之前存在一个问题：摄影指导跑完之后，编剧填好的镜头字段会被意外清空。现在已修复，摄影指导只会填自己负责的部分，不会覆盖编剧已经填好的内容。

### 3. 字段自动校验 + 出错自动让模型修正

加入了两个自动检查环节：

**编剧初稿完成后**，系统会检查每一幕是否都填了必要的镜头字段（比如景别、切换方式）。如果有遗漏，会用自然语言告诉编剧 AI 哪几幕、哪个字段有问题，让它重新生成，最多重试两次。

**摄影指导完成后**，系统会检查所有镜头字段的值是否合法（比如景别只能是"全景/中景/近景"等固定选项，不能填其他词）。如果有不合规的值，同样会告诉模型具体是哪幕、哪个字段、填了什么不对的内容，让它修正。

**站位规划**那步也加了类似的检查：如果 AI 规划完站位后，发现有哪个位置的区域名或角色名是空的，会告诉 AI 哪里有问题并要求重填，最多重试两次。

这样做的好处是：出了问题不再是直接报错中断，而是先给模型一次明确的纠错机会，整体成功率更高。

### 对你有什么影响？

- 使用方式没有变化。
- 生成的剧本镜头信息更完整、更规范。
- 遇到字段填写问题时，系统会自动修正，减少需要手动重跑的情况。
- 前端场景信息展示更准确，区域与锚点的含义与实际一致。
